/**
 * 배열 .pop()
 * 마지막 배열의 값을 뽑음
 * LIFO(Last In First Out)
 * FILO(First In Last Out)
 */
const destination = ['런던', '파리', '로마'];

console.log(destination);

destination.push('테스트');
console.log(destination);

console.log(destination.pop());
console.log(destination);
